// Display-side caps. Server enforces the real numbers via env.ts
// (STEP_MAX_PER_ROW, STEP_BUDGET_CENTS_PER_ROW). If you change those
// envs, update these too so the drawer's budget meter matches.
export const DISPLAY_STEP_MAX_PER_ROW = 5;
export const DISPLAY_STEP_BUDGET_CENTS_PER_ROW = 10;
